# GitHub repository metadata

Apply once the repo is pushed (account: copyleftdev).

## About (description)

> Deterministic semantic diff for N artifacts — consensus, drift, and outliers with inspectable evidence. Zig, zero deps.

## Topics

Chosen from measured topic traffic (2026-07-07): high-reach (`cli` 103k repos,
`developer-tools` 42k), mid-band identity (`static-analysis` 5.7k, `zig` 5.2k,
`configuration-management` 2.5k, `diff` 2.3k), and low-competition niches
where WTD can rank immediately (`drift-detection` 710, `semantic-diff` 33).

```
zig diff semantic-diff drift-detection cli command-line-tool developer-tools
static-analysis configuration-management devops outlier-detection ai-tools
deterministic blake3 json yaml
```

## Apply with gh

```sh
gh repo edit copyleftdev/whatthediff \
  --description "Deterministic semantic diff for N artifacts — consensus, drift, and outliers with inspectable evidence. Zig, zero deps." \
  --add-topic zig --add-topic diff --add-topic semantic-diff \
  --add-topic drift-detection --add-topic cli --add-topic command-line-tool \
  --add-topic developer-tools --add-topic static-analysis \
  --add-topic configuration-management --add-topic devops \
  --add-topic outlier-detection --add-topic ai-tools \
  --add-topic deterministic --add-topic blake3 \
  --add-topic json --add-topic yaml
```
